// import React from 'react';
// import PropTypes from 'prop-types';
// import { getCompanyName } from '../../utils/getCompanyName';

// const CompanysRoleDefinition = ({
//   namesType,
//   companysName: { sender, recipient },
//   contractor,
// }) => {
//     const { data, error } = useGetContractorByIdQuery(contractor?.value);
//      const senderName = getCompanyName(sender ?? data, namesType);
//      const recipientName = getCompanyName(recipient ?? data, namesType);
//   return <div>CompanysRoleDefinition</div>;
// };

// CompanysRoleDefinition.propTypes = {};

// export default CompanysRoleDefinition;
